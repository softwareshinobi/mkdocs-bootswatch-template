services:

    mkdocs-bootswatch-template:

        container_name: mkdocs-bootswatch-template

        image: softwareshinobi/mkdocs-bootswatch-template

        build: 

            context: .

            dockerfile: Dockerfile
      
        restart: unless-stopped
  
        ports:

            - 8000:80
